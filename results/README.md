# Measured evidence

This directory retains only compact outputs generated by commands documented in
the root README. Raw media, caches, downloaded weights, and large benchmark
artifacts are ignored.

`measured_cpu.json` and `annotated_demo.jpg` are created only after real local
execution. Their provenance and environment are documented in the root README.
